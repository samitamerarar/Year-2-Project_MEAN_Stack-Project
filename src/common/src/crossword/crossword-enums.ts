/**
 * Grid Word
 */
export enum Direction {
    horizontal,
    vertical,
}

export enum Owner {
    none,
    player,
    opponent,
}

/**
 * Game Configs
 */
export enum Difficulty {
    easy,
    normal,
    hard,
}

export enum GameMode {
    Classic,
    Dynamic,
}
