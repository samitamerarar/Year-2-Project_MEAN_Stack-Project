/**
 * Integer part = lap number
 * Fractional part = fraction of current lap completed
 */
export type Progression = number;
/**
 * Fraction of currentLap completed [0, 1)
 */
export type LapProgression = number;
